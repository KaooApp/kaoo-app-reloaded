import type { RestaurantInfo } from '@/types/restaurant';

export const defaultShopId = '323' as RestaurantInfo['shopid'];
export const defaultCurrency = '€';

// colors
export const successColor = '#69C779';
