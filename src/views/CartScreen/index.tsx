import { Text } from 'react-native-paper';
import type { FC } from 'react';

import AppBarLayout from '@/components/layout/AppBarLayout';

const CartScreen: FC = () => (
    <AppBarLayout title="CartScreen" settings>
        <Text>CartScreen</Text>
    </AppBarLayout>
);

export default CartScreen;
